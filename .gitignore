# ── Rust build output ─────────────────────────────────────────────────────────
target/
**/*.rs.bk
*.pdb

# ── Compiled Python extensions — NEVER push pre-built binaries ───────────────
# Developer installs from source: maturin develop --release
*.so
*.pyd
*.dylib
_native.abi3.*
stratarouter_core*.so
stratarouter_core*.pyd

# ── Rust backup / draft files ─────────────────────────────────────────────────
*.rs.backup
ffi.rs.backup
backup_router.rs

# ── Python bytecode / packaging ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.whl

# ── Virtualenvs ───────────────────────────────────────────────────────────────
venv/
venv311/
.venv/
env/
ENV/
.Python

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# !! SECRETS — never commit these !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
.env
.env.*
*.pem
*.key
*.cert
*.p12
PyPI-Recovery-Codes-*
agentdyne_codes.txt
agentdyne_full_export.sql

# ── Development artefacts ────────────────────────────────────────────────────
backup_setup.py
# Root-level throwaway scripts and scratch files (never in OSS repo)
py2.py
v1.py
test.py
sample.sh
test2.sh
# Virtual-environment artefacts
pyvenv.cfg
lib64
.python-version

# ── Private scripts (never in OSS repo) ──────────────────────────────────────
capture_claude.sh
fix_auth_and_push.sh
core2_git.sh
core_git.sh
run_coverage.sh

# ── Codebase snapshots ────────────────────────────────────────────────────────
stratarouter_codebase_*.txt

# ── Build artifacts ───────────────────────────────────────────────────────────
*.zip
*.tar.gz
cargo_warnings.txt

# ── Test / coverage output ────────────────────────────────────────────────────
.coverage
coverage/
htmlcov/
.pytest_cache/
*.lcov
tarpaulin-report.html
core/coverage/

# ── Docs build ────────────────────────────────────────────────────────────────
site/
docs/_build/

# Dev artefacts (never commit these)
oss_fix.sh
test3.sh
test2.sh
backup_setup.py
setup.py
"New Text Document.txt"
*.rs_*
DELETE_THESE_FILES.txt
dist/
target/
